# Part 8 — Claim Boundary

## Allowed claims

The following statements are supported by the frozen Gate 8.4 / 8.4b result:

- No defensible `N_crit` was found under the frozen Gate 8.4 rule.
- DKW and censor-related conditions passed as specified, while the plateau persistence requirement did not.
- Gate 8.4b reproduced the NULL outcome under its own frozen follow-up conditions.
- The parent Gate 8.4 NULL remains immutable and is not retroactively reinterpreted.

## Not allowed

The result does not justify saying:

- the trajectory map is structurally impossible;
- the map is validated;
- a different threshold would have rescued the result;
- the latent breakpoint tail beyond `gamma_max` is identified;
- representation transfer has been demonstrated;
- the controller should now be tuned or deployed.

## Status language

Use:

> Under the frozen Gate 8.4 decision rule, no defensible critical reference-population size was identified. The result is NULL.

Do not replace NULL with "almost passed," "failed because the grid was too sparse," or any post-hoc rescue interpretation.

## Next experimental boundary

Q0.0 is a separate question:

> Does trajectory structure survive a real change of representation?

Any Q0.0 result must be reported independently of Gate 8.4.
